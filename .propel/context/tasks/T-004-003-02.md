# Task: Implement Pagination and View Toggle

**Task ID:** T-004-003-02  
**User Story:** US-004-003 - Document History & Library Management  
**Epic:** EP-004 - Results Display & Entity Management  
**Type:** Frontend  
**Estimate:** 4 hours  
**Priority:** P1 - High

---

## Description

Implement pagination controls and view toggle between grid (cards) and list (table) views for the History page.

---

## Acceptance Criteria

- [ ] Grid view (cards) and List view (table) toggle
- [ ] View preference persisted in localStorage
- [ ] Pagination with:
  - Page numbers
  - Previous/Next buttons
  - Items-per-page selector (10, 25, 50, 100)
- [ ] Page state in URL for bookmarking

---

## Technical Implementation

```tsx
export const Pagination = ({ page, totalPages, pageSize, onPageChange, onPageSizeChange }) => (
  <div className="flex items-center justify-between py-4">
    <Select value={pageSize} onValueChange={onPageSizeChange}>
      <SelectItem value={10}>10 per page</SelectItem>
      <SelectItem value={25}>25 per page</SelectItem>
      <SelectItem value={50}>50 per page</SelectItem>
      <SelectItem value={100}>100 per page</SelectItem>
    </Select>
    
    <div className="flex items-center space-x-2">
      <Button onClick={() => onPageChange(page - 1)} disabled={page <= 1}>
        Previous
      </Button>
      {/* Page numbers */}
      <span>Page {page} of {totalPages}</span>
      <Button onClick={() => onPageChange(page + 1)} disabled={page >= totalPages}>
        Next
      </Button>
    </div>
  </div>
);
```

---

## Definition of Done

- [ ] View toggle works (grid/list)
- [ ] View persists in localStorage
- [ ] Pagination controls render
- [ ] Page size selector works
- [ ] Page state in URL
- [ ] Navigation works correctly
