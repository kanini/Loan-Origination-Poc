# Task: Integrate react-pdf with Zoom and Navigation

**Task ID:** T-004-002-01  
**User Story:** US-004-002 - PDF Viewer & Split Panel Layout  
**Epic:** EP-004 - Results Display & Entity Management  
**Type:** Frontend  
**Estimate:** 6 hours  
**Priority:** P1 - High

---

## Description

Integrate react-pdf library for PDF rendering with zoom controls and page navigation on the Results page.

---

## Acceptance Criteria

- [ ] react-pdf renders PDF documents
- [ ] Zoom controls: Zoom In, Zoom Out, Fit to Width, Fit to Page
- [ ] Page navigation: Previous, Next, "Page X of Y"
- [ ] Full-screen option
- [ ] PDF loads from document URL
- [ ] Loading state while PDF fetches

---

## Technical Implementation

```tsx
import { Document, Page, pdfjs } from 'react-pdf';

pdfjs.GlobalWorkerOptions.workerSrc = `//unpkg.com/pdfjs-dist@${pdfjs.version}/build/pdf.worker.min.js`;

export const PDFViewer = ({ url }) => {
  const [numPages, setNumPages] = useState(0);
  const [pageNumber, setPageNumber] = useState(1);
  const [scale, setScale] = useState(1);

  return (
    <div className="h-full flex flex-col">
      <div className="flex items-center justify-between p-2 border-b">
        <div className="flex items-center space-x-2">
          <Button size="sm" onClick={() => setScale(s => s - 0.1)}>-</Button>
          <span>{Math.round(scale * 100)}%</span>
          <Button size="sm" onClick={() => setScale(s => s + 0.1)}>+</Button>
        </div>
        <div className="flex items-center space-x-2">
          <Button onClick={() => setPageNumber(p => p - 1)} disabled={pageNumber <= 1}>
            Previous
          </Button>
          <span>Page {pageNumber} of {numPages}</span>
          <Button onClick={() => setPageNumber(p => p + 1)} disabled={pageNumber >= numPages}>
            Next
          </Button>
        </div>
      </div>
      <div className="flex-1 overflow-auto">
        <Document file={url} onLoadSuccess={({ numPages }) => setNumPages(numPages)}>
          <Page pageNumber={pageNumber} scale={scale} />
        </Document>
      </div>
    </div>
  );
};
```

---

## Definition of Done

- [ ] react-pdf installed and configured
- [ ] PDF renders correctly
- [ ] Zoom In/Out works
- [ ] Fit to Width/Page works
- [ ] Page navigation works
- [ ] Full-screen option available
- [ ] Loading state shown
