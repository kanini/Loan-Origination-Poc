# Task: Implement CSV and JSON Export Functions

**Task ID:** T-005-002-01  
**User Story:** US-005-002 - Multi-Format Export (Excel, CSV, PDF)  
**Epic:** EP-005 - AI Chatbot & Export Functionality  
**Type:** Frontend  
**Estimate:** 4 hours  
**Priority:** P1 - High

---

## Description

Implement JSON and CSV export functions with client-side file generation and download using Blob API.

---

## Acceptance Criteria

- [ ] Export dropdown with format options
- [ ] JSON export downloads extraction data
- [ ] CSV export with flattened entity data
- [ ] Proper file naming pattern
- [ ] Special characters escaped in CSV
- [ ] Loading indicator during export

---

## Technical Implementation

```tsx
const exportToJSON = (data: EntityData, filename: string) => {
  const json = JSON.stringify(data, null, 2);
  const blob = new Blob([json], { type: 'application/json' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = `${filename}_entities.json`;
  a.click();
  URL.revokeObjectURL(url);
};

const exportToCSV = (data: EntityData, filename: string) => {
  const rows = flattenEntities(data);
  const csv = rows.map(row => 
    row.map(cell => `"${String(cell).replace(/"/g, '""')}"`).join(',')
  ).join('\n');
  const blob = new Blob([csv], { type: 'text/csv' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = `${filename}_entities.csv`;
  a.click();
  URL.revokeObjectURL(url);
};
```

---

## Definition of Done

- [ ] JSON export works
- [ ] CSV export with flattened data
- [ ] Files download correctly
- [ ] Proper file naming
- [ ] Special characters handled
- [ ] Loading state during export
